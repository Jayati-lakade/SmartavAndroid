<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="android:Theme.Holo.Light.DarkActionBar">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->

    </style>

    <!--
    <style name="loginTheme" parent="android:Theme.Holo.Light.NoActionBar">
        <item name="android:buttonStyle">@style/docoButtonStyle</item>
        
    </style>
    -->


    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">

        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
        <item name="android:buttonStyle">@style/docoButtonStyle</item>
        <item name="android:editTextStyle">@style/docoEditTextStyle</item>
        <item name="android:actionBarStyle">@style/docoActionBarStyle</item>
        <item name="android:textViewStyle">@style/docoTextStyle</item>
    </style>

    <style name="docoButtonStyle" parent="android:style/Widget.Button">
        <item name="android:background">@drawable/button_selector</item>
        <item name="android:height">35dp</item>
        <item name="android:textSize">@dimen/smallText</item>
    </style>

    <style name="docoEditTextStyle" parent="@android:style/Widget.EditText">
        <item name="android:background">@drawable/bg_edittext</item>
        <item name="android:textSize">@dimen/smallText</item>
    </style>

    <style name="docoTextStyle" parent="@android:style/Widget.TextView">
        <item name="android:textSize">@dimen/smallText</item>
    </style>

    <style name="docoActionBarStyle" parent="@android:style/Widget.Holo.Light.ActionBar">
        <item name="android:background">@drawable/top_bar</item>
        <item name="android:textSize">@dimen/smallText</item>
        <item name="android:textStyle">bold</item>
        <!-- <item name="android:height">50dp</item> -->
    </style>

</resources>